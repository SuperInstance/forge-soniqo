[package]
name = "forge-soniqo"
version = "0.1.0"
edition = "2021"
description = "Audio decomposition for Plato agents"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
